export const fetchObjectBBox = async () => {
  throw new Error('objectBBoxService is deprecated and must not be used. Frontend must use the selected ticket crop box as the preview reference and only send ratios.');
};
